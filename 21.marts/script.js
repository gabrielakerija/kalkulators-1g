const button = document.querySelector(.js-plus)

const plus = (cipars1, cipars2, cipars3)=> {
   return cipars1 + cipars2 + cipars3
}

button.addEventListener('click', ()return => {
return plus (1,2,3)
})